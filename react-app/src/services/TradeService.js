import { hostNameUrl } from "../config/api";
import axios from "axios";

export const getTradesFiveDayExecuted = (date) => {
  // TODO: uncomment when API is working
  // const trades = axios.get(`${hostNameUrl}/trades/{date}`);
  // return trades;
  return null;
}

